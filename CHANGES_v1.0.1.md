# نمایش‌یار اندروید — تغییرات نسخه ۱.۰.۱

## محدوده تغییرات تأییدشده

| بخش | تغییر اعمال‌شده |
|---|---|
| نمایش مبلغ | تمام مبلغ‌ها صرفاً در رابط اندروید از ریال ورودی JSON به تومان تبدیل و با جداکننده هزارگان کامل نمایش داده می‌شوند. نمایش فشرده «میلیون ریال» حذف شد. |
| خانه | عنوان «مجموع مانده قابل وصول» به «تراز بدهی/طلب» تغییر کرد. مقدار آن با فرمول بیلان ویندوز محاسبه می‌شود: جمع طلب از مشتری‌ها منهای جمع بدهی به مشتری‌ها. |
| جزئیات تراز | با لمس کارت تراز، پنجره‌ای کوچک شامل جمع بدهی به مشتری‌ها با رنگ قرمز و جمع طلب از مشتری‌ها با رنگ سبز نمایش داده می‌شود. |
| کاشی‌های آماری | سه کاشی «مشتری»، «فاکتور باز» و «کالا» از صفحه خانه حذف شدند. |
| فاکتورهای اخیر | بخش فاکتورهای اخیر به محل کاشی‌های حذف‌شده منتقل شد، ۱۰ فاکتور را نمایش می‌دهد و فقط بر اساس شماره فاکتور از بزرگ به کوچک مرتب می‌شود. در صورت برابری شماره، تاریخ صدور معیار دوم است. |
| وضعیت سررسید | عبارت و برچسب «سررسید گذشته» از تمام کارت‌های فاکتور و فیلتر فاکتورها حذف شد. سایر وضعیت‌های احتمالی بدون تغییر باقی مانده‌اند. |

## تغییرنکرده‌ها

> هیچ تغییری در نسخه ویندوز، صادرکننده `db.py` ویندوز، ساختار JSON یا مقادیر ذخیره‌شده JSON اعمال نشده است.

فایل‌های JSON قبلی همچنان قابل ورود هستند و خروجی JSON جدیدی از ویندوز نیاز نیست.

## کنترل کیفیت

تحلیل ایستا Flutter بدون خطا اجرا شد، هر سه آزمون موجود پروژه با موفقیت گذشتند و APK آزمایشی قابل نصب ساخته شد.
