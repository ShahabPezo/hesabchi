# حسابچی — نسخه ۱.۲.۳

این انتشار فقط پنج اصلاح متنی و نمایشی تأییدشده را دارد. منطق فاکتور، مشتری، داده محلی، HCH/JSON، مرتب‌سازی و چیدمان‌های دیگر تغییر نکرده‌اند.

| مورد | اصلاح |
|---:|---|
| ۱ | هر نام کسب‌وکار واردشده از JSON که شامل «کارگاه» باشد، فقط در رابط Android به «کارخانه» نمایش داده می‌شود؛ مقدار خام JSON تغییر نمی‌کند. |
| ۲ | هر نام کسب‌وکار نمایشی حاوی «نمایش یار» یا «نمایشیار» در رابط به «حسابچی» تبدیل می‌شود. نام قدیمی پایین تنظیمات نیز حذف شد. |
| ۳ | متن جست‌وجوی مشتری به «نام، شماره تلفن مشتری» تغییر کرد. |
| ۴ | نسخه نمایش‌داده‌شده در صفحه آغاز و پایین تنظیمات از منبع مشترک استفاده می‌کند و در این انتشار هر دو «۱.۲.۳» هستند. |
| ۵ | عبارت «طراح و سازنده: شهاب» به پایین تب تنظیمات افزوده شد. |
