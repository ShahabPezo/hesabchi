# حسابچی — نسخه ۱.۵.۱

این انتشار اصلاحی فقط ناسازگاری داده‌های حقوق افراد میان صادرکننده ویندوز و Android را رفع می‌کند. هیچ تغییر UI یا تغییر نامرتبط در تب‌های خانه، مشتری‌ها، قیمت‌ها، آمار، بیشتر، فاکتورها، تنظیمات یا همگام‌سازی GitHub اعمال نشده است.

| مورد | اصلاح |
|---:|---|
| ۱ | Android اکنون HCH قدیمی ویندوز را که `billingYear` را متن سال مانند `"1405"` و `billingMonth` را متن ماه مانند `"05 - مرداد"` صادر می‌کرد، به‌صورت کنترل‌شده به سال/ماه عددی تبدیل می‌کند. |
| ۲ | رکورد واریزی حقوقی که هر دو فیلد دوره آن خالی است، چون در محاسبه بازه حقوق نقشی ندارد، نادیده گرفته می‌شود. رکورد دارای دوره ناقص یا نامعتبر همچنان با خطای فارسی رد می‌شود. |
| ۳ | HCH واقعی موجود در GitHub یک سابقه پروفایل با مانده صفر و بدون تاریخ آخرین تسویه داشت. Android این حالت بی‌اثر را می‌پذیرد، اما مانده غیرصفرِ بدون تاریخ همچنان نامعتبر است. محاسبه حقوق نیز چنین سابقه‌ای را وارد مانده تسویه نمی‌کند. |
| ۴ | صادرکننده ویندوز اکنون `billingYear` و `billingMonth` واریزی حقوق را به شکل عددی canonical صادر می‌کند و رکورد فاقد دوره را اصلاً صادر نمی‌کند. همچنین مهاجرت جدول حقوق شماره تماس را حفظ می‌کند. |
| ۵ | آزمون Android برای هر دو فرم قدیم/جدید دوره واریزی و آزمون Windows برای صادرات دوره عددی افزوده/به‌روز شد. |

## علت پیام‌های کاربر

پیام دستی «فیلد billingMonth … باید عددی باشد» از تفاوت نوع خروجی ویندوز و انتظار Android ایجاد شده بود. همگام‌سازی GitHub نیز اینترنتی نبود؛ فایل دانلود می‌شد اما همان HCH در مرحله اعتبارسنجی حقوق افراد رد می‌شد و UI طبق طراحی پیام عمومی شبکه/به‌روزرسانی نشان می‌داد.

## کنترل کیفیت

پس از اصلاح، همه آزمون‌های Android با موفقیت اجرا شدند و HCH فعلی GitHub مستقیماً با parser واقعی Android تحلیل شد. آزمون صادرات Windows نیز موفق شد. APK نسخه `۱.۵.۱` با `versionCode 16` ساخته شد.
