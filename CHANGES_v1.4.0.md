# حسابچی — نسخه ۱.۴.۰

این انتشار فقط قابلیت تأییدشده **«دریافت به‌روزرسانی» از GitHub** را اضافه می‌کند. فایل‌های ویندوز و تب‌های مشتری‌ها، فاکتورها، قیمت‌ها و آمار تغییر عملکردی نکرده‌اند.

| مورد | تغییر |
|---:|---|
| ۱ | گزینه «دریافت به‌روزرسانی» در بخش داده‌های تنظیمات افزوده شد. در حالت بدون داده نیز همین گزینه برای دریافت نخستین HCH در دسترس است. |
| ۲ | کاربر Android هیچ تنظیماتی برای GitHub وارد نمی‌کند؛ فقط دیالوگ «در حال به‌روزرسانی اطلاعات…» و پیام نتیجه ساده می‌بیند. |
| ۳ | ابتدا متادیتای کوچک `latest_meta.json` بررسی می‌شود. اگر خروجی تازه‌تر نباشد، HCH کامل دانلود نمی‌شود و پیام «اطلاعات همین حالا به‌روز است.» نمایش می‌گیرد. |
| ۴ | HCH فقط پس از دریافت کامل، کنترل اندازه و اعتبارسنجی parser فعلی وارد می‌شود. جایگزینی snapshot محلی اتمی است؛ در خطای شبکه یا فایل نامعتبر، داده قبلی دستگاه باقی می‌ماند. |
| ۵ | زمان آخرین همگام‌سازی موفق زیر نام کارخانه در تب خانه نمایش داده می‌شود. |
| ۶ | فقط مجوز Android `INTERNET` و وابستگی HTTP لازم افزوده شد. |
| ۷ | مقدار محرمانه توکن در سورس، تغییرنامه، بسته تحویل و گزارش‌ها ثبت نشده است؛ فقط هنگام ساخت APK از مقدار ساخت تزریق می‌شود. |

## کنترل کیفیت

خواندن متادیتای واقعی GitHub با توکن فقط‌خواندنی با موفقیت کنترل شد. تحلیل Flutter بدون خطا انجام شد، آزمون‌های پروژه شامل سناریوهای دریافت موفق، داده بدون تغییر، خطای شبکه و HCH نامعتبر موفق بودند و APK آزمایشی نسخه `۱.۴.۰` با `versionCode 14` ساخته شد.
