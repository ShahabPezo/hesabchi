# حسابچی — نسخه ۱.۲.۰

این نسخه پنج مشکل گزارش‌شده را هدف قرار می‌دهد.

| مورد | اصلاح انجام‌شده |
|---|---|
| صفحه آغاز | صفحه سفید و چرخنده ساده با یک صفحه آغاز کامل جایگزین شد: پس‌زمینه گرادیانی برند، لوگوی دستگاه پرس کارتن، نام حسابچی، توضیح کوتاه، نسخه ۱.۲.۰، نشان پیشرفت و عبارت «طراحی و ساخت: شهاب». |
| بازکردن JSON | مسیر `ACTION_VIEW` برای URIهای `content://` و `file://` افزوده شد. علاوه بر MIMEهای JSON، یک مسیر جایگزین برای MIMEهای نامشخص فایل‌منیجرهای Android 8.1 نیز ثبت شده است. برنامه فقط فایل‌های با نام `.json` یا MIME JSON را می‌خواند. |
| فاکتورهای خانه | لمس هر فاکتور اخیر، پنجره کوچک جزئیات فاکتور را باز می‌کند. |
| فاکتورهای مشتری | لمس هر فاکتور در جزئیات مشتری، همان پنجره کوچک جزئیات را باز می‌کند. |
| Recent Apps سامسونگ | تم پنجره، زمینه بومی، activity task و تنظیمات حالت شب تغییر داده شدند تا Android در نمای Recent Apps یک زمینه روشن و snapshot قابل‌گرفتن داشته باشد، نه صفحه سیاه. |

پنجره کوچک جزئیات فاکتور شامل نام مشتری، شماره فاکتور، تاریخ صدور، نوع عملیات، وزن خالص، قیمت واحد و مبلغ کل است. وضعیت، سررسید، پرداخت و باقی‌مانده در آن نمایش داده نمی‌شوند.

> برای آزمون فایل‌منیجر روی J7 Prime، ابتدا نسخه قبلی حسابچی را به نسخه ۱.۲.۰ به‌روزرسانی کنید. سپس فایل JSON را یک بار از «My Files» با لمس مستقیم باز کنید. اگر سامسونگ کادر انتخاب برنامه نشان داد، «حسابچی» را انتخاب کنید. چون این نسخه به‌صورت گسترده‌تر برای `ACTION_VIEW` ثبت شده، ممکن است حسابچی در فهرست بازکردن انواع دیگری از فایل هم دیده شود؛ اما فقط JSON را وارد می‌کند. مسیر «اشتراک‌گذاری» نیز بدون تغییر فعال است.
