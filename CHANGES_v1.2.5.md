# حسابچی — نسخه ۱.۲.۵

این انتشار فقط تغییرات تأییدشده مربوط به تشخیص نوع دیتابیس HCH و تأیید خروج از برنامه را اعمال می‌کند. فایل‌های ویندوز، قرارداد قبلی داده و بخش‌های نامرتبط Android تغییر نکرده‌اند.

| مورد | اصلاح |
|---:|---|
| ۱ | کلید سطح بالای `scope` در فایل HCH خوانده می‌شود. مقدار `all` به «کل فاکتورها» و مقدار `range` به «از تاریخ شروع تا تاریخ پایان» تبدیل می‌شود. |
| ۲ | فایل‌های HCH قدیمی که کلید `scope` ندارند، همچنان قابل ورود هستند و «کل فاکتورها» نمایش داده می‌شود. |
| ۳ | در صفحه خانه، نوع دیتابیس با فونت کوچک زیر نام کارخانه نمایش داده می‌شود. |
| ۴ | در تنظیمات، عنوان بخش به «اطلاعات دیتابیس» تغییر کرد و ردیف «نسخه قرارداد داده» با «نوع دیتابیس» جایگزین شد. |
| ۵ | هنگام تلاش برای خروج از برنامه، پیام «ببندم؟ کارت تموم شد؟» با گزینه‌های «آره تمومه» و «نه!» نمایش داده می‌شود؛ فقط انتخاب گزینه تأیید، برنامه را می‌بندد. |

## کنترل کیفیت

تحلیل Flutter بدون خطا انجام شد، هر چهار آزمون پروژه با موفقیت گذشتند و APK آزمایشی نسخه `۱.۲.۵` با `versionCode 9` ساخته شد.
