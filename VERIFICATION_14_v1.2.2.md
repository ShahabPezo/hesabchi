# حسابچی ۱.۲.۲ — کنترل ۱۴ مورد گزارش‌شده

این نسخه فقط فایل‌های مرتبط با صفحه آغاز، خانه، مشتری‌ها و فاکتورها را تغییر می‌دهد. داده JSON، ورودی HCH، منطق مانده حساب، قیمت‌ها، تنظیمات و دیگر بخش‌ها دست‌نخورده‌اند.

| شماره | مورد تأییدشده | وضعیت در ۱.۲.۲ |
|---:|---|---|
| ۱ | مدت نمایش لودینگ دوم | صفحه آغاز کامل Flutter حداقل **۵ ثانیه** نمایش داده می‌شود. |
| ۲ | عنوان فاکتورهای خانه | عنوان به **«۱۰ فاکتور اخیر مشتری‌ها»** تغییر یافت. |
| ۳ | لمس فاکتورهای اخیر در خانه | لمس هر کارت، پنجره کوچک جزئیات فاکتور را باز می‌کند. |
| ۴ | شناسه زیر نام مشتری | شناسه فنی مشتری دیگر در فهرست نمایش داده نمی‌شود. |
| ۵ | دایره حرف اول مشتری | Avatar و دایره حرف اول از فهرست مشتری‌ها حذف شد. |
| ۶ | متن جست‌وجوی مشتری | متن به **«جست‌وجوی نام، شماره تلفن مشتری»** تغییر یافت. |
| ۷ | مرتب‌سازی مشتری‌ها | کنترل جمع‌وجور مرتب‌سازی با چهار حالت **مبلغ ↑، مبلغ ↓، الف ↑، الف ↓** افزوده شد. مرتب‌سازی مبلغ با مانده علامت‌دار انجام می‌شود. |
| ۸ | عنوان مانده در جزئیات مشتری | پس از مبلغ، عنوان کوچک **«بدهی به مشتری»**، **«تسویه»** یا **«طلب از مشتری»** همراه با رنگ مناسب نمایش داده می‌شود. |
| ۹ | فیلترها و تگ‌های پرداخت فاکتور | از رابط فاکتور پنهان شده‌اند؛ منطق و داده وضعیت در مدل حذف نشده است. |
| ۱۰ | کنترل‌های جدید زیر جست‌وجوی فاکتور | سه کنترل **«همه»**، **«نام مشتری»** و **«شماره فاکتور»** برای تعیین دامنه جست‌وجو افزوده شدند. |
| ۱۱ | ترتیب فاکتورها | فاکتورها براساس شماره عددی فاکتور به‌صورت نزولی مرتب می‌شوند؛ بالاترین شماره در ابتدای فهرست است. |
| ۱۲ | عنوان مبلغ کارت فاکتور | عنوان به **«مبلغ فاکتور»** تغییر یافت و عدد بدون رنگ‌بندی وضعیت نمایش داده می‌شود. |
| ۱۳ | باقی‌مانده کارت فاکتور | بخش «باقی‌مانده» از کارت فاکتور پنهان شد؛ مدل داده حذف نشده است. |
| ۱۴ | صفحه جزئیات فاکتور | نام مشتری و شماره فاکتور حفظ شده‌اند. پیش از خط فقط تاریخ صدور، نوع عملیات، وزن خالص و قیمت واحد دیده می‌شود؛ پس از خط فقط مبلغ کل نمایش دارد. تگ، سررسید، پرداخت‌شده، باقی‌مانده و بخش اقلام فاکتور حذف شده‌اند. |

## کنترل کیفیت

تحلیل Flutter بدون خطا انجام شد، هر سه آزمون پروژه موفق بود و APK آزمایشی نسخه `۱.۲.۲` با `versionCode 6` ساخته شد.
