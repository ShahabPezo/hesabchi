# حسابچی — نسخه ۱.۳.۰

این انتشار قابلیت تأییدشده «آمار» را به برنامه اضافه می‌کند. فایل‌های ویندوز و تب‌های مشتری‌ها، فاکتورها و قیمت‌ها تغییر نکرده‌اند.

| مورد | تغییر |
|---:|---|
| ۱ | تب پنجم نوار پایین از «تنظیمات» به **«آمار»** تغییر کرد؛ تعداد مقصدهای اصلی همان پنج مورد باقی مانده است. |
| ۲ | تنظیمات از طریق آیکون چرخ‌دنده در سمت چپ نوار بالای برنامه در دسترس است. |
| ۳ | داده اختیاری `monthlyStatus` از فایل HCH خوانده می‌شود. فایل‌های قدیمی بدون این کلید همچنان بدون خطا وارد می‌شوند. |
| ۴ | تب آمار دارای انتخاب‌گر ماه است و آخرین ماه موجود را به‌صورت پیش‌فرض نمایش می‌دهد. |
| ۵ | صفحه آمار با چیدمان موبایل‌محور و کارت‌محور، وضعیت بار مشتری، وضعیت بار فروشگاه، وضعیت کلی ماهانه و پنج مشتری برتر را نمایش می‌دهد. |
| ۶ | مبلغ‌های داده `monthlyStatus` که مستقیماً تومان هستند، بدون تقسیم بر ۱۰ نمایش داده می‌شوند. وزن‌ها با واحد کیلوگرم و درصدها با علامت درصد نمایش داده می‌شوند. |
| ۷ | اگر وزن پایه صفر باشد، فی کارتن با «—» نمایش داده می‌شود تا صفر گمراه‌کننده نشان داده نشود. |
| ۸ | برای جلوگیری از خطا، آزمون‌های سازگاری HCH قدیمی و جدید، رندر تب آمار، مسیر چرخ‌دنده تنظیمات و ناوبری تب آمار افزوده شد. |

## کنترل کیفیت

تحلیل Flutter بدون خطا انجام شد، همه آزمون‌های پروژه با موفقیت گذشتند و APK آزمایشی نسخه `۱.۳.۰` با `versionCode 12` ساخته شد.
