# حسابچی — نسخه ۱.۲.۴

این انتشار فقط چهار اصلاح تأییدشده را اعمال می‌کند. منطق داده، فایل HCH/JSON، صفحه‌های فاکتور، تنظیمات، برند و دیگر بخش‌های برنامه تغییر نکرده‌اند.

| مورد | اصلاح |
|---:|---|
| ۱ | متن جست‌وجوی تب قیمت‌ها به «جست‌وجوی مشتری یا نوع کالا» تغییر کرد. |
| ۲ | زمان آخرین به‌روزرسانی قیمت‌ها فقط با تاریخ جلالی نمایش داده می‌شود و ساعت حذف شد. |
| ۳ | فاکتورهای بخش جزئیات مشتری بر اساس شماره عددی فاکتور، به‌صورت نزولی مرتب می‌شوند؛ بالاترین شماره ابتدای فهرست است. |
| ۴ | زیر کاشی تراز بدهی/طلب در صفحه خانه دو کاشی اضافه شد: کاشی سبز «بزرگترین بدهکاریمون» برای مشتری با بیشترین طلب از ما، و کاشی قرمز «بزرگترین طلبکاریمون» برای مشتری با بیشترین بدهی ما به او. هر دو از مانده‌های موجود مشتریان محاسبه می‌شوند. |

## کنترل کیفیت

تحلیل Flutter بدون خطا انجام شد، هر سه آزمون پروژه موفق بود و APK آزمایشی نسخه `۱.۲.۴` با `versionCode 8` ساخته شد.
