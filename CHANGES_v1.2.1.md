# حسابچی — نسخه ۱.۲.۱

این نسخه، خروجی اختصاصی حسابچی و اصلاح تجربه صفحه آغاز را اضافه می‌کند.

| بخش | تغییر انجام‌شده |
|---|---|
| فایل خروجی ویندوز | دکمه خروجی به «خروجی حسابچی» تغییر کرده است. فایل با پسوند `.hch` و نام پیشنهادی `hesabchi_data.hch` ذخیره می‌شود؛ **محتوای فایل همچنان همان JSON نسخه ۱ است** و هیچ داده یا فرمولی تغییر نکرده است. |
| بازکردن فایل Android | حسابچی فایل‌های `.hch` و JSON قدیمی را از انتخاب فایل، اشتراک‌گذاری و لمس مستقیم فایل‌منیجر می‌پذیرد. برای `.hch`، MIME اختصاصی `application/x-hesabchi` و مسیرهای جایگزین Android 8.1 ثبت شده‌اند. |
| صفحه آغاز بومی | صفحه اول Android سبک شد و فقط گرادیان برند را نشان می‌دهد؛ تصویر لوگو از آن حذف شد تا مدت نمایش کوتاه‌تر شود و مربع سفید تکراری نداشته باشد. |
| صفحه آغاز حسابچی | صفحه دوم کامل با لوگوی شفاف واقعی، نام، نسخه، توضیح، نشان بارگذاری و «طراحی و ساخت: شهاب» نمایش داده می‌شود. حداقل مدت نمایش آن ۳٫۲ ثانیه است تا فرصت دیده‌شدن داشته باشد. |
| لوگو | شبکه سفید/خاکستریِ به‌اشتباه ذخیره‌شده داخل PNG حذف شد؛ دارایی‌های Android اکنون آلفای واقعی دارند. |

## روش استفاده

1. در ویندوز، در تب «ثبت و ویرایش فاکتورها»، روی **«خروجی حسابچی»** بزنید و فایل `.hch` را ذخیره کنید.
2. در Android، فایل HCH را از فایل‌منیجر لمس کنید و در صورت نمایش انتخابگر برنامه، **حسابچی** را انتخاب کنید. از تلگرام نیز می‌توان فایل را با «اشتراک‌گذاری» به حسابچی فرستاد.
3. فایل‌های JSON پیشین همچنان برای سازگاری قابل ورودند؛ اما برای ارسال به کاربران دیگر، خروجی HCH را استفاده کنید.
